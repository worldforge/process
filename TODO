Make Connection::recv a template, so it can check for an specific
op, instead of messing about checking by id string.

Order Effective from amazon or somewhere.

Sort how some thoughts on type conversions in Atlas::Objects. Particularly, what happens
when someone does a dynamic cast down the tree illegally.

Make the waitFor code all use serial/ref no.s properlly.

Add tests for appearance and disappearance ops in lobby

Add TO attr to Talk ops. Add test to ensure everything still works if TO
is not set. Add test to see whether server detect misset TO.

Out-of-Game

Test invalid password 
Test duplicate login

[ multi-room stuff ]
	Room creation
	Room join
	Room leave

	Chat in multiple (created rooms)

Character look to unknown character

In-Game

Move self
Chat
